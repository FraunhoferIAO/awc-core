{
  "name": "awc-core-examples",
  "version": "0.1.0",
  "description": "Examples for awc-core. This package is just to manage additional depencencies for examples. It should never appear in any repository on its own.",
  "private": true,
  "dependencies": {
    "@webcomponents/shadycss": "^1.0.6",
    "@webcomponents/webcomponentsjs": "^1.0.14",
    "lite-server": "^2.3.0"
  },
  "scripts": {
    "start": "lite-server"
  }
}
